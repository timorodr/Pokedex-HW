<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OG Pokedex</title>
    <%- include("./css.ejs") %>
</head>
<body>
    <h1>Pokedex</h1>

    <nav>
        <a href="/pokemons/new">Add a new pokemon</a>
    </nav>
    <div class="poke-container">
        <ul>
            <% for (let i = 0; i < pokemons.length; i++){ %>
                <a href="pokemons/<%= i %>"><img src= <%= pokemons[i].img %>> </a>
                    <div class="dex-id"> <%= pokemons[i].id %></div>
                    <div class="dex-name"> <%= pokemons[i].name %></div>
                    

                    <form action="/pokemons/<%= i %>?_method=DELETE" method="post">
                        <input type="submit" value="delete">
                    </form>

            <% } %>
        </ul>
    </div>    
</body>
</html>


