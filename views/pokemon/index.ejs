<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OG Pokedex</title>
    <%- include("./css.ejs") %>
</head>
<body>
    <h1>Pokedex</h1>

    <nav>
        <a href="/pokemon/new">Add a new pokemon</a>
    </nav>
    <div class="poke-container">
        <ul>
            <% for (let i = 0; i < pokemon.length; i++){ %>
                <a href="pokemon/<%= i %>"><img src= <%= pokemon[i].img %>> </a>
                    <div class="dex-id"> <%= pokemon[i].id %></div>

            <% } %>
        </ul>
    </div>    
</body>
</html>


